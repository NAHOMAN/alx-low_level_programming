#include<stdio.h>
#include "main.h"

void _puts(char *s);

/**
 * main - Entry point
 * puts - programing
 * _puts: programing
 * str: mulitple puzzle
 * Return: Always 0 (Success)
 */
void main(void)
{
	_puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
