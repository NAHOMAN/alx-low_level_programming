#include "main.h"

int _sqrt_recursion(int n);
int find_sqrt(int num, int root)
	
/**
 * find_sqrt - find the square root of the number
 * @num: the number to find the square
 * @root: the root to be tested
 * Return: if the number has a square root
 */
int find_sqrt(int num, int root) 
{
	if ((root * root) == num)
		return (root);
}
