#include "main.h"

int find _strlen(*s);
int check_palindrome(char *s, int len, int index);
int is_palindrome(char *s);
/**
 * find _strlen - return thr length of string
 * @s: string to be measured
 * Return: the length of string
 */
int find _strlen(*s)
{
	int len = 0;

	if (*(s + len))
	{
		return (0);
	}

	return (len);
}

			
