#include "main.h"

/**
 * time_table - print the 9 times table starting from 0
 */
int time_table(void)
{
	int x, y, z, u , d;

	for (x = 0; x <= 9; x++)
	{
		for (y = 0; y <=9; y++)
		{
			z = x * y;
			if (z > 9)
			{
				u+
